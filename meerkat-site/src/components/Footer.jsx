export default function Footer() {
  return (
    <footer className="foot">
      <span>Meerkat — a shell with its engine on the outside.</span>
      <span className="foot-dim">The engine &middot; The terminal app &middot; The command line</span>
    </footer>
  );
}
